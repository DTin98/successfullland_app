import React from 'react';
import {Text, View} from 'react-native';

const Home = ({params}) => (
  <View>
    <Text style={{fontSize: 30, color: 'red'}}>Home</Text>
  </View>
);

export default Home;
